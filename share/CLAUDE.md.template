<!-- Generated by `worktrees {{name}}` on {{date}}. Edit freely — re-runs leave this file alone. To refresh from template: `rm CLAUDE.md && worktrees {{name}}`. -->

# Worktree group: {{name}}

This directory is a worktrees-tool–generated group rooted at `$WORKTREE_ROOT`
(`~/Desktop/worktrees/{{name}}`). It contains five sibling worktrees, one
per source repo:

- liveblocks
- liveblocks-backend
- admin
- liveblocks.io
- zenrouter

You're being run from inside one of these worktrees. When subordinate
`CLAUDE.md` files (the ones in each sub-repo) reference paths under
`~/Projects/liveblocks/`, **treat those as relative to this group** — i.e.,
edit `$WORKTREE_ROOT/liveblocks-backend/foo.ts`, not
`~/Projects/liveblocks/liveblocks-backend/foo.ts`. The source checkouts in
`~/Projects/liveblocks/` are off-limits; never edit them.

Cross-repo changes belong in **this group's worktrees only**.
