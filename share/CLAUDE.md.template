<!-- Generated by `worktrees init {{name}}` on {{date}}. Edit freely — re-runs leave this file alone. To refresh from template: `rm CLAUDE.md && worktrees init {{name}}`. -->

# Worktree group: {{name}}

This directory is a worktrees-tool–generated group rooted at `$WORKTREE_ROOT`
(`{{group_dir}}`). It belongs to the `{{workspace}}` workspace and holds
{{count}} sibling worktree(s), one per repo:

{{repos}}

You're being run from inside one of these worktrees. When subordinate
`CLAUDE.md` files (the ones in each sub-repo) reference paths under
`{{source_root}}/`, **treat those as relative to this group** — i.e. edit
`$WORKTREE_ROOT/<repo>/foo.ts`, not `{{source_root}}/<repo>/foo.ts`. The source
checkouts in `{{source_root}}/` are off-limits; never edit them.

The group may cover only part of the workspace. If a change turns out to need a
repo that isn't listed above, say so rather than editing the source checkout —
`worktrees add <repo>` brings it into the group.

All changes belong in **this group's worktrees only**.
